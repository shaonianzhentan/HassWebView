namespace HassWebView.AndroidService
{
    /// <summary>
    /// Represents an action button to be displayed on a notification.
    /// </summary>
    public record NotificationAction(string Id, string Title);
}
